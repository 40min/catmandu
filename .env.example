TELEGRAM_BOT_TOKEN=your_bot_token_here

# Optional: Path to store Telegram update offset (default: .data/update_id.txt)
# UPDATE_ID_FILE_PATH=.data/update_id.txt

# Optional: Directory containing cattackle modules (default: cattackles)
# CATTACKLES_DIR=cattackles

# Optional: Directory for chat logs (default: logs/chats)
# CHAT_LOGS_DIR=logs/chats

# Optional: Message accumulator configuration (defaults: 100, 1000)
# MAX_MESSAGES_PER_CHAT=100
# MAX_MESSAGE_LENGTH=1000

# Optional: Logging configuration (default: INFO)
# LOG_LEVEL=INFO

# AI API configuration (for echo cattackle joke functionality)
# OpenAI is the primary model, Gemini is fallback - at least one is required

# OpenAI API configuration (primary for echo cattackle)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-nano

# Google Gemini API configuration (fallback for echo cattackle)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite-preview-06-17

# =============================================================================
# AUDIO PROCESSING CONFIGURATION
# =============================================================================
# Audio processing allows the bot to handle voice messages and audio files
# by transcribing them to text using OpenAI's Whisper API and improving
# the transcription quality using the configured OpenAI model.

# Enable/disable audio processing features (default: false)
# When enabled, requires OPENAI_API_KEY to be set
# AUDIO_PROCESSING_ENABLED=false

# OpenAI API key for audio processing (required if AUDIO_PROCESSING_ENABLED=true)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your_openai_api_key_here

# Audio processing limits to control costs and prevent abuse
# Maximum audio file size in MB (default: 25, max: 50 due to Telegram limits)
# MAX_AUDIO_FILE_SIZE_MB=25

# Maximum audio duration in minutes (default: 10, max: 60)
# MAX_AUDIO_DURATION_MINUTES=10

# =============================================================================
# COST TRACKING CONFIGURATION
# =============================================================================
# Configure cost tracking for audio processing to monitor OpenAI API usage

# Whisper API cost per minute of audio (default: $0.006 as of 2024)
# Update this value if OpenAI changes their pricing
# WHISPER_COST_PER_MINUTE=0.006

# OpenAI model input cost per 1M tokens (default: $0.05 as of 2025)
# OPENAI_GPT_NANO_INPUT_COST_PER_1M_TOKENS=0.05

# OpenAI model output cost per 1M tokens (default: $0.40 as of 2025)
# OPENAI_GPT_NANO_OUTPUT_COST_PER_1M_TOKENS=0.40

# Directory for cost tracking logs (default: logs/costs)
# Cost logs help you monitor spending and generate reports
# COST_LOGS_DIR=logs/costs

# =============================================================================
# NOTION CATTACKLE CONFIGURATION
# =============================================================================
# Configure user access to Notion integration
# Each user needs both a Notion integration token and a parent page/database ID
#
# IMPORTANT: Use the management tools instead of editing manually:
#   make add-notion-user USER="John Doe" TOKEN="your_token" PAGE_ID="your_page_id"
#   make list-notion-users
#   python scripts/manage_notion_users.py --help
#
# Environment Variable Format:
#   NOTION__USER__{USERNAME}__TOKEN=notion_integration_token
#   NOTION__USER__{USERNAME}__PARENT_PAGE_ID=page_or_database_id
#
# Username Rules:
#   - Will be converted to UPPERCASE with underscores
#   - "John Doe" becomes "JOHN_DOE" in environment variables
#   - Only letters and underscores allowed (no numbers or special chars)
#
# Getting Notion Tokens:
#   1. Go to https://www.notion.so/my-integrations
#   2. Create a new integration
#   3. Copy the "Internal Integration Token" (starts with "secret_")
#   4. Share your target page/database with the integration
#
# Finding Page/Database IDs:
#   - Page: Copy URL, ID is after the last "/"
#   - Database: Copy URL, ID is before the "?"
#
# Example configurations (uncomment and modify):
# NOTION__USER__JOHN_DOE__TOKEN=secret_notion_integration_token_1
# NOTION__USER__JOHN_DOE__PARENT_PAGE_ID=page_id_or_database_id_1
# NOTION__USER__JANE_SMITH__TOKEN=secret_notion_integration_token_2
# NOTION__USER__JANE_SMITH__PARENT_PAGE_ID=page_id_or_database_id_2
