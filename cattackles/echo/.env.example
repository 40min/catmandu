# OpenAI API configuration (primary for joke functionality)
# Required: OpenAI API key for joke functionality
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI model to use (default: configured in settings)
OPENAI_MODEL=gpt-5-nano

# Google Gemini API configuration (fallback for joke functionality)
# Optional: Gemini API key for joke functionality
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini model to use
GEMINI_MODEL=gemini-2.5-flash-lite-preview-06-17

# Optional: MCP server port (default: 8001)
# MCP_SERVER_PORT=8001

# Optional: Logging level (default: INFO)
# LOG_LEVEL=INFO
