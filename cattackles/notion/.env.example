# Notion Cattackle Environment Variables
# Copy this file to .env and configure the values for your environment

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the MCP server (default: 8002)
# MCP_SERVER_PORT=8002

# Host to bind the server to (default: 0.0.0.0)
# HOST=0.0.0.0

# =============================================================================
# Logging Configuration
# =============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# LOG_LEVEL=INFO

# =============================================================================
# Notion API Configuration
# =============================================================================

# Notion API base URL (default: https://api.notion.com/v1)
# NOTION_API_BASE_URL=https://api.notion.com/v1

# =============================================================================
# User Configuration
# =============================================================================

# IMPORTANT: Notion tokens and workspace paths are configured
# in the user configuration module (src/notion/config/user_config.py),
# not through environment variables for security reasons.
#
# To configure users:
# 1. Edit src/notion/config/user_config.py
# 2. Add entries to USER_CONFIGS dictionary with:
#    - username: Telegram username
#    - token: Notion integration token
#    - parent_page_id: Notion page or database ID where daily pages will be created
#
# Example:
# USER_CONFIGS = {
#     "your_telegram_username": {
#         "token": "secret_notion_integration_token",
#         "parent_page_id": "your_notion_page_or_database_id"
#     }
# }

# =============================================================================
# Development Configuration
# =============================================================================

# Python path for development (automatically set in container)
# PYTHONPATH=/app

# Enable debug mode for development
# DEBUG=false
